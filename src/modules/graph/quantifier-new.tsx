import React, { useState, useEffect, useMemo, useCallback } from "react"
import { AST } from "@/parser"
type Props = {
  x: number
  y: number
  quantifier: AST.Quantifier
}
const QuantifierNode: React.FC<Props> = () => {
  return <></>
}
export default QuantifierNode
